Private Sub CarregarDados()
  Dim i As Integer
  Dim x As Integer
  Dim str1 As String
'  Dim strValores() As String
  Dim RS As ADODB.Recordset
  
  i = rsgGridDados.Fields.Count
  ReDim strValores(i) As String
  
  Set oCEFIOF4 = oCUTUTIL.RetClasse("CEFIOF4.Negocio")
  oCEFIOF4.ConexaoBD oConexao
  
  Set RS = oCEFIOF4.PesquisarLista
    
  RS.MoveFirst
  Do While RS.EOF = False
  'CARREGANDO COM OS NOMES
'    rsgGridDados.AddNew
'    rsgGridDados!EF4DESCRICAO_DDEA = RS!EF4DESCRICAO_DDEA
'    rsgGridDados!EF4VALOR_PIS_DDEA = RS!EF4VALOR_PIS_DDEA
'    rsgGridDados!EF4VALOR_COF_DDEA = RS!EF4VALOR_COF_DDEA
'    rsgGridDados.Update
  'CARREGANDO GENERICO A PARTIR DOS CAMPOS DEFINIDOS NO GRID
    rsgGridDados.AddNew 'AddNew com arrays[campos, valores] nos parametros nao funcionou
    For x = 0 To RS.Fields.Count - 1
      For i = 0 To rsgGridDados.Fields.Count - 1
        If RS.Fields(x).Name = rsgGridDados.Fields(i).Name Then
          str1 = RS.Fields(x).Value
          rsgGridDados.Fields(i).Value = str1
          'strValores(i) = RS.Fields(x).Value
          x = x + 1
        End If
      Next
    Next
    rsgGridDados.Update
'USAR ARRAY CARREGADO
'    If strValores(0) <> "" Then
'      rsgGridDados.AddNew
'      For i = 0 To UBound(strValores) - 1
'        rsgGridDados.Fields(i).Value = strValores(i)
'        strValores(i) = ""
'      Next
'      rsgGridDados.Update
'    End If
    RS.MoveNext
  Loop
  
End Sub